import { SectionCard } from "@/components/shared/section-card";

export function MatchupForm() {
  return (
    <SectionCard
      variant="dense"
      eyebrow="Model lab"
      title="Unavailable"
      description="The model lab is hidden from the active product until it has a fully real workflow behind it."
    />
  );
}
